#include <stdio.h>

int isSafe(int lab[][], int i, int j){
    return ( && lab[i][j] == 0);
}

int saida(int lab[][], int Movei[], int Movej[]){
if
  for (int k = 0; k < 5; k++) {
        int next_i = i + Movei[k];
        int next_j = j + Movej[k];
        if (isSafe(next_i, next_j, lab)){
            lab[next_x][next_y] = 1;
            if (saida(next_i, next_j, lab, Movei, yMovej) == 1)
                return 1;
            else
                lab[next_i][next_j] = 0; // backtracking
        }
    }
}

int main(){
  int i,j, inicio;
  int lab[20][18]

  for (i=0; i<20; i++)
    for (j=0; j<18; j++)
      scanf("%d", &lab[i][j]);

  int Movei[] = {0,  0, 1, -1};
  int Movej[] = {1, -1, 0,  0};

  for (i=0; i<20; i++)
    if (lab[i][0] == 0)
      inicio = i;

  lab[inicio][0] = 1;
  saida(lab, Movei, Movej, inicio);

  return 0;
}
